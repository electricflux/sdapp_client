<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.parking.dashboard"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="8" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name="com.parking.dashboard.activity.DashboardActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.parking.add.AddCapture"
                  android:label="@string/app_name">
        </activity>
        
        <activity android:name="com.parking.manage.Manage"
                  android:label="@string/app_name">
        </activity>

        <activity android:name="com.parking.personalbests.PersonalBests"
                  android:label="@string/app_name">
        </activity>

        <activity android:name="com.parking.view.ViewAll"
                  android:label="@string/app_name">
        </activity>

    </application>
</manifest>